# 小地图导航默认接入

2026-09-19，MuMu `127.0.0.1:16384`，MaaFramework 5.12.3，控制器 1280×720。

## 接入范围

猫咪日记的追猫、控制所跨层、城堡楼梯、昆仑冰坡前后定位，以及基础单步、巴尔沃基巡路、
星天 200 胜巡路，默认使用同一套导航会话。今后使用 `Navigator.position/move` 的巡路也默认接入。
传送和楼层变化调用 `reset_navigation`，不能仅凭同名标题复用楼层模型。
菜单切换、传送确认、猫交互和战斗结算仍由各任务原有页面状态判断。
温泉与已确认的无小地图连接路保留场景标志检查。

使用方式和 GUI 选项不变，用户无需另选实验入口。旧 `locate_legacy/position_legacy` 只供历史截图诊断，
正式任务不会调用；小图不足时回退的是本次整图多帧观测。

## 修复的兼容问题

- 羽毛目标两帧更新并投影；视野外目标有时效，接近或丢失时重新观测，不能用旧坐标宣布完成。
- 地图模型剔除移动羽毛和粉色怪物图例。纳兹里克支持固定图例校正整体平移，
  初始整图校验后可学习非标准地图比例，后续每帧独立配准。
- 长地图标题使用已实测的 780px 装饰识别范围，解决巢穴标题尾端超出原 650px 范围的问题。
- 地图采集中消失、进入战斗、开关图之间发生战斗时，丢弃不完整样本，由原任务恢复。
  已完成同图模型可以复用；首次建模不混合战斗前后的不同场景。
- 到达复核只采集独立整图坐标，不在驻怪点重新提取道路。巢穴巡路复用已验证的底部道路中心线。
- 月度移动被战斗截断时接受同方向的部分位移；仍拒绝明显反向位移和侧向偏离。
  门和传送标签点击识别框中心，避免随机落在识别框边缘。
- 控制所不能仅凭 10px 路点容差等待出口；须看到门按钮，必要时只在已验证的同一横路末端微调。
  小图连续校验失败的稀疏地图暂按整图观测推进，每次尝试恢复小图，原任务的步数和位移保护继续生效。
  控制所补充现场金色出口按钮样式，保留原白色样式；点击后仍必须核验新楼层与实际落点。

## 验证范围

- 巴尔沃基正式巡路：5 次滑动、2 次开图、5 次小图更新，终点独立误差 0.27px。
- 纳兹里克东侧斜坡：4 次滑动、2 次开图、4 次小图更新，终点独立误差 0.69px。
  复核了大图原点随坡高偏移、坐标归一化及约 0.95 倍比例。
- 控制所完整前往研究中心深处：入口 → 研究中心下层 → 同名上层，两次门交互后分别核验落点。
  最终位置 `(496.74,461.94)`，符合 `(498,462)` 的落点检查。入口 9 步、7 次开图；下层 10 步、11 次开图，
  上层重新建模 1 次。此图小地图静态证据少，目前以整图回退为主，不能宣称减少了下层开图次数。
- 晓之塔一层到巢穴的既有路线已实走；塔内主要由小地图推进，一层终点误差约 0.52px。
  巢穴往返和遇敌恢复也已实走，保留真实结算计数。巢穴的小图非常稀疏，遇敌和图例变化时仍频繁回退整图，
  不能宣称这张图已达到普通城镇相同的开图减少幅度。
- 宫殿、米格兰斯城 2 楼、伊西亚三组真实截图的羽毛识别与投影回放通过，误差约 0.7～4.0px；
  覆盖空白小地图不能复用上一个羽毛目标的反例。

实测汇总保存在 `output/minimap-integration/`，原始日志与截图在 `debug/minimap/2026-09-19/`。

最终 180 项逻辑与定位回归通过，另有 3 组实际 OCR、3 组羽毛投影和金色门按钮正负样本回放通过。
Schema、MaaTools 资源检查及差异格式检查通过。运行包已更新到 `install/`，原生 Agent 握手、注册、
正常退出检查通过；运行包独立 Agent 实际执行 `NavigationBaruokiRoute` 和 `CatDiary` 均成功。
前者终点独立误差 0.36px，后者确认当天三个槽位已完成并保留日记页，没有重复寻猫。

本次开始时当天三个猫槽均已完成（4 枚印章），因此没有新的活动猫可供完整追逐。
本次不能作为新一轮猫咪日记全自动完成的证据，也没有重新跑满 200 胜。
其他分辨率、未知楼层或全部猫咪地图尚未验证。

## 2026-09-20 刷新任务补测

运行包独立 Agent 分段完成蛇头梅兹基塔、伊刹那 → 炼狱界、佐见，共 4 次猫交互，最终到达
`CatDiaryComplete` 并确认三个红爪印。伊刹那最终成功路线滑动 10 次、整图建模/校准 2 次、小图更新 10 次；
佐见最终成功路线滑动 10 次、整图建模/校准 4 次、小图更新 12 次，包含小图呼吸环暂时缺失后的整图恢复。
蛇头无小地图，使用独立的单步场景检查；炼狱界这次没有进入小地图追逐阶段，不计作其导航验证。

修复了佐见移动羽毛形成伪角色脉动圆的问题：整图定位排除同帧已识别的羽毛框，仍要求剩余候选唯一。
伊刹那配置完整地图名，以现有灰度 OCR 避免起点“巳/已”误读锁定错误标题，并为遮挡羽毛记录有限搜索路线。
本次属于边实测边修复后的分段完成，尚未验证修复后从当天初始三槽位一次无干预完成。

## 2026-09-21 初始任务整轮验证

使用包含上述修复的运行包独立 Agent，从模拟器桌面自动启动游戏，再读取三个均未完成的猫槽，
一次连续无干预完成猫神神社遗迹、帕西法尔宫殿、劳拉巨蛋城 → 炼狱界，共 4 次交互。
最终达到 `CatDiaryComplete`、三个槽位均显示红爪印，退出码 0。印章全程为 7/7，按每次日记线索或槽位变化确认进度。

| 地图 | 滑动次数 | 整图建模/校准次数 | 小图更新次数 |
| --- | --- | --- | --- |
| 猫神神社遗迹 | 14 | 4 | 14（含 2 次轮廓配准） |
| 帕西法尔宫殿 1 楼 | 4 | 1 | 3 |
| 劳拉巨蛋城 | 8 | 2 | 8 |

炼狱界到达后即完成交互，没有进入小地图追踪，不计作其导航验证。
本轮 26 次追踪滑动共使用 7 次整图建模/校准，复核跨时代、跨区域传送后的会话重置和连续小图导航。
上述次数来自 Agent 的导航日志，不包含世界地图传送菜单；宫殿最后一次移动后直接识别交互按钮，因此少一次小图更新。

原始日志、首尾截图及按日记状态分段的 `summary.json` 保存在 `debug/cat/2026-09-21/initial/`。
运行前保存源码与资源哈希，结束后确认没有变化；当天未修改任务代码或资源，未重新运行单元测试。
这次补齐了修复后从初始三槽位连续完成的实机证据，但入口仍为程序调用运行包原生客户端及 Agent，
尚未验证 GUI 前端按钮启动。四个地点均已有交互记录，累计覆盖仍为 32/74。

## 2026-09-22 遮挡与交互时序修复

当天初始任务为劳拉巨蛋城、安格尔、扎博，劳拉交互后更新为柯尼姆。
分段修复共完成 4 次交互，印章 `0 → 1 → 2 → 3 → 4`，最终三个红爪印并到达 `CatDiaryComplete`。

- 柯尼姆刷新整图时，角色环与相邻图例遮住路端，新提取道路不包含当前角色位置。
  仅当全名、机位偏移不变，且本次整图独立坐标符合旧道路时，复用同一会话此前确认的道路。
  `fixed1/` 实机再次触发此分支，并完成交互；首次建模或无旧道路支持仍停止。
- 安格尔的交互按钮在导航采样期间出现，继续开图后角色环与羽毛重叠导致定位失败。
  追猫阶段在采样及开图前优先检查可操作主界面和场景按钮；交互实际回日记后，仍由原流程复核任务变化。
  其他导航任务、跨层前往目标和地图展开状态不执行该交互，不修改识别阈值。
- 扎博仅录入实机区域图上的上方局部横路、左侧连接路和下一条街道一段，重新传送后验证左、下、右路线。

| 成功交互所在分段 | 地图 | 实际滑动 | 整图建模/校准 | 小图更新 |
| --- | --- | --- | --- | --- |
| `initial/` | 劳拉巨蛋城 | 8 | 2 | 8 |
| `fixed1/` | 魔兽村落柯尼姆 | 7 | 2 | 7 |
| `fixed2/` | 再生村落安格尔 | 6 | 1 | 5 |
| `fixed3/` | 沙漠村落扎博 | 5 | 1 | 4 |

表格只计最终完成交互的各段，不含修复前失败尝试；采样时已完成交互的最后一步没有小图位置更新。
原始日志、截图和分段汇总保存在 `debug/cat/2026-09-22/`；152 项相关单元测试与回归通过。
本次使用运行包独立 Agent 分段完成，尚未验证最终版本从当天初始三项一次无干预完成或 GUI 按钮入口。
累计地点覆盖仍为 32/74；扎博仅验证本次标定路段，其他街道和分支未验证。

## 2026-09-23 初始任务整轮验证与阿克托尔覆盖

使用包含 2026-09-22 修复的运行包独立 Agent，从游戏标题画面启动并处理公告，
从三项均未完成的初始日记一次连续无干预完成阿克托尔、埃尔吉昂·伽玛区、莎蒂拉体育场 → 伊杜依斯。
印章 `4 → 5 → 6 → 7 → 1`，最终三个槽位均为红爪印，达到 `CatDiaryComplete`，退出码 0。

| 地图 | 实际滑动 | 整图建模/校准 | 小图更新 |
| --- | --- | --- | --- |
| 水之都阿克托尔 | 10 | 2 | 8 |
| 埃尔吉昂·伽玛区 | 4 | 1 | 3 |
| 莎蒂拉体育场 | 5 | 2 | 4 |
| 隐居村落伊杜依斯 | 11 | 2 | 9 |

共 30 次滑动、7 次整图建模/校准、24 次小图更新；校准次数不含世界地图传送菜单。
体育场的小图更新包含 1 次轮廓配准和 2 次混合配准。采样期间已完成交互的最后一步不再产生位置更新。
阿克托尔从落点沿右、上、左路线追猫，使用现场提取道路，未新增固定中心线或图片模板。
首次完成该地点的日记交互，累计覆盖从 32/74 增至 33/74；仅验证本次路线，其他街道和出口未覆盖。

日志、首尾截图、版本快照和 `summary.json` 保留于 `debug/cat/2026-09-23/initial/`。
当天未修改任务代码或资源，首尾哈希一致；仅补充验证文档，未重新运行单元测试。
这次验证了昨日修复版本从初始三项连续完成新一轮任务，入口仍为程序调用运行包原生客户端及独立 Agent，
尚未验证 GUI 前端按钮启动整轮。

## 2026-09-24 启动恢复与路口微调修复

启动流程在读取存档并关闭公告后停留于梅纳斯试炼选关页，补充已知页面的识别返回分支后恢复主界面，
详见 [启动并登录](startup.md)。运行包独立 Agent 随后从三项均未完成的日记连续完成伊斯亚、巴尔沃基和米格兰斯城二楼。
印章 `1 → 2 → 3 → 4`，最终三个红爪印并到达 `CatDiaryComplete`，退出码 0。

| 地图 | 实际滑动 | 整图建模/校准 | 小图更新 |
| --- | --- | --- | --- |
| 空中城郭伊斯亚 | 15 | 2 | 13 |
| 巴尔沃基 | 3 | 2 | 1 |
| 米格兰斯城二楼 | 6 | 2（含上楼前一楼） | 6（含 1 次轮廓配准） |

巴尔沃基小图角色环不可确认时回到整图定位；城堡自动核验一楼、楼梯及二楼落点。
该轮仍使用原导航版本。伊斯亚的路径先包含 4px 纵向投影、再接 4px 横向对齐和完整下行路，
旧判断在忽略纵向投影后跳过路口检查，实际产生数次左右微调。修复后继续检查新得到的横向短段，
保留原有 8px 对齐容差与后续纵路超过 8px 的条件，未扩大路网或识别阈值。

更新运行包后，单独从伊斯亚落点 `(746.80,280.36)` 导航到已确认的下方横路 `(480,360)`：
在 `(596.80,279.63)` 处直接下行，下一帧确认 `(590.04,358.31)`，没有再次左右微调。
全程 11 次滑动、2 次展开整图、11 次小图更新、0 次中途重校准。
终点整图 `(485.95,358.75)` 与末次小图观测相差 1.45px，达到 `NavigationMiniMapComplete`，退出码 0。
随后再运行猫咪日记，确认仍为三个红爪、4 枚印章，未产生新的交互。

证据在 `debug/cat/2026-09-24/fixed1/`、`isiya-route/`；80 项猫咪日记测试、73 项导航回归和启动截图回放通过。
当天属于启动修复后的整轮日记与导航修复后的单独路段回归，最终版本尚未从初始三项再跑整轮，GUI 按钮入口未验证。
地点覆盖仍为 33/74；只复核上述路段，不扩展为整张地图验证。

## 2026-09-25 帕德列全名与体育场海报恢复

初始线索为炼狱界、帕德列和埃尔吉昂·伽玛区。炼狱界完成后，帕德列已成功传送，
但显式 `map_names` 仍为简称“帕德列”，与实机完整标题“古代树之村帕德列”不相等，导航在建模前停止。
只修正该地点的完整名称，保留精确校验与既有道路；真实标题回放复现旧配置失败、新配置通过，并拒绝简称与附加楼层。

帕德列修复后完成交互，下一条线索为莎蒂拉体育场。体育场到达猫附近后进入了两页海报说明，
旧普通对话识别要求右上“对话记录”和底部边框，无法处理顶部说明框，交互超时后停下。
依据实机采集补充海报边框和两页首行完整文字，沿用原有点击位置及合计 20 页上限。
排查时逐页确认返回方式，随后重新打开海报首屏；最终运行包自动翻过两页，重新读取日记并传送，
连续完成体育场与埃尔吉昂，达到 `CatDiaryComplete`、退出码 0。

| 成功交互分段 | 地图 | 实际滑动 | 整图建模/校准 | 小图更新 |
| --- | --- | --- | --- | --- |
| `initial/` | 炼狱界 | 0 | 0 | 0 |
| `fixed1/` | 古代树之村帕德列 | 4 | 2 | 4 |
| `fixed2/` | 莎蒂拉体育场 | 5 | 3 | 7（含 3 次轮廓配准） |
| `fixed2/` | 埃尔吉昂·伽玛区 | 3 | 1 | 2 |

表格不含失败的体育场尝试，也不将中途无滑动的定位轮计作移动。炼狱界没有进入小地图追踪，不计作新增导航验证。
共 4 次交互，印章 `4 → 5 → 6 → 7 → 1`，最终三个红爪印。累计覆盖仍为 33/74。
日志、截图、版本快照和分段汇总位于 `debug/cat/2026-09-25/`；海报实际恢复动作记录在 `fixed2/dialogue-actions.json`。
80 项猫咪日记测试、6 组真实 OCR 和海报原生回放、Schema、maa-tools 检查通过。
本日为分段修复后完成，最终版本从剩余两项运行至结束；初始三项整轮及 GUI 前端按钮入口仍未验证。

## 2026-09-26 博物馆图书区域验证与直达纠正

初始运行完成那古萨无、纳兹里克、猫神神社遗迹和拉托尔后，线索刷新到“博物馆里有好多书”。
地点库把“马克米纳尔图书馆”作为世界地图直达点，有限扫描结束后停止。
首次处理遗漏了尼尔瓦子菜单的“博物馆 图书区”，误判为需要逐层走入，做了以下跨区验证。
用户提供菜单截图后已改成直接传送；下面的七段进场及其测试数据仅为历史记录，不再是当前执行流程。
旧版在标题相同的馆内区域分别保存道路、落点和出口，不以相同标题判定换层成功。

每段持续读取小地图；信息不足时回到整图定位。出口先做独立整图复核，再识别对应门或楼梯图标。
四层楼梯点击后，操作栏曾先消失再恢复，旧等待因此过早打开地图并取消行走。
博物馆分支改为等待实际黑屏过渡，再等新场景可操作，最后核验新落点；原有其他跨层分支保持原等待条件。
每段 40 步、连续三步无预期位移、用户停止及任务期限保护仍生效。

旧跨层版本的运行包独立 Agent 从剩余图书区域、柯尼姆两个槽位重新开始：重新传送尼尔瓦，
连续完成七段进场和图书区追猫，再传送柯尼姆完成最后一只，达到 `CatDiaryComplete`、退出码 0。

| 进场区域 | 实际移动 | 出口整图复核 |
| --- | --- | --- |
| 浮空城尼尔瓦 | 12 | 1 |
| 博物馆馆外 | 0 | 1 |
| 馆内第 1 段 | 12 | 1 |
| 馆内第 2 段 | 24 | 1 |
| 馆内第 3 段 | 16 | 1 |
| 馆内第 4 段 | 21 | 1 |
| 馆内第 5 段 | 22 | 1 |

进场共 107 次移动；7 次出口复核中，小图预测与独立整图位置误差为 0.71～2.09px。
馆外传入位置已经接近入口，复核后直接点击；没有把零移动误计为未验证该入口。

| 成功交互地点 | 追猫滑动 | 进场滑动 | 成功建模/校准 | 出口整图复核 | 小图更新 |
| --- | --- | --- | --- | --- | --- |
| 辰之国那古萨无 | 8 | 0 | 3 | 0 | 6 |
| 影之镇纳兹里克 | 14 | 0 | 3 | 0 | 12 |
| 猫神神社遗迹 | 13 | 0 | 5 | 0 | 11 |
| 火之村拉托尔 | 0 | 0 | 0 | 0 | 0 |
| 博物馆图书区域（含进场） | 16 | 107 | 17 | 7 | 118 |
| 魔兽村落柯尼姆 | 6 | 0 | 2 | 0 | 4 |

以上按两次正式任务日志统计，不含调试采样、失败的世界地图扫描和传送菜单拖动；
建模/校准与出口复核分开计数。小图更新合计 151 次，其中 100 次为轮廓配准。
共 6 次交互，印章 `1 → 2 → 3 → 4 → 5 → 6 → 7`，最终三个红爪印。
首次完成图书区域寻猫，覆盖从 33/74 增至 34/74；图书区更深处同名区域、其他出口及全地图通用导航未验证。

证据位于 `debug/cat/2026-09-26/initial/`、`fixed1/`；后者保留修复版从传送开始的完整进场及剩余任务完成记录。
旧跨层版本通过 80 项原有猫咪日记测试、11 项博物馆边界测试、14 个原生识别正反例、图书区标题 OCR、Schema 和 maa-tools 检查。
这是分段修复后的当日完成，尚未从初始三项重跑，也未验证 GUI 前端按钮启动。

### 同日纠正：从尼尔瓦子菜单直接传送

正确入口是未来本土大陆“尼尔瓦”下的“博物馆 图书区”，确认框显示“即将移动到博物馆 图书区”。
目录现在用 `world_entry` 选择父入口，最终传送名只匹配图书区，保留落地后的完整标题核验及图书区道路。
删除七段进场实现、馆外及五段馆内道路、门和楼梯模板、博物馆专用黑屏识别；道路区域总数由 32 调整为 26。

实机自动复测还发现：菜单动画中的旧帧会引发第二次点击，图书区标签恰与确认框的“是”位置重叠，
导致未完成文字核验就开始传送，随后等待确认超时。修复为子菜单只选一次，等待确认框识别；
缺少直达项、确认文字不符或等待超时均失败，不改走绕楼层流程。

`debug/cat/2026-09-26/direct-library/` 使用 `install/agent/cat_diary.py`、运行包资源和原生运行时验证：

| 用例 | 结果 |
| --- | --- |
| 从尼尔瓦选择图书区 | 直达成功，落点 `(801.72,263.68)`，0 次进场移动 |
| 已在图书区时再次选择图书区 | 直达成功，落点 `(801.50,263.46)`，0 次进场移动 |
| 从传送点沿图书区道路到下方走廊 | 16 次移动、16 次小图更新；终点整图 `(454.53,455.24)` |

目标为 `(450,456)`，终点距离约 4.59px，小图与独立整图误差 1.92px。
终点复核通过、测试退出码 0；进场从原先 107 次移动降为 0。
80 项原有测试、5 项直达边界测试、5 组原生识别/确认回放通过。
本次为已完成当日任务后的传送与区域导航验证，不是新增猫交互；覆盖仍为 34/74，GUI 按钮入口未验证。

## 2026-09-27 佐见移动交互修复

初始日记为埃尔吉昂·伽玛区、炼狱界、海之国佐见，三项均未完成，印章 7/7。
初次运行完成前两项，佐见追踪 9 步后因移动羽毛按钮连续点击 10 次落空而停止。
真实截图及原生日志显示按钮识别正确，但全画面识别的延迟使点击坐标过期；五组采样中全画面耗时
0.436～0.451 秒，局部重识别耗时 0.048～0.051 秒，四组按钮已横移 40～42px，另一组已消失。

交互现在先用全画面发现按钮，再用当前截图局部重识别并点击新框；丢失按钮或主界面控件时不复用旧框。
保持模板阈值、点击上限、交互超时及返回日记校验。修复后的运行包独立 Agent 重新传送佐见，
连续 10 步追踪后完成一次点击；局部识别框已从旧框 x=839 更新至 x=777，随后进入日记，最后一槽红爪确认。
达到 `CatDiaryComplete`，退出码 0。

| 成功交互地点 | 移动次数 | 成功建模/校准 | 小图更新 |
| --- | --- | --- | --- |
| 埃尔吉昂·伽玛区 | 3 | 1 | 2 |
| 炼狱界 | 0 | 0 | 0 |
| 海之国佐见（修复后） | 10 | 3 | 9 |

成功分段合计 13 次移动、4 次成功建模/校准、11 次小图更新，不含失败佐见段的 9 次移动、3 次校准、8 次小图更新。
埃尔吉昂小图角色环暂不可确认时进入恢复流程，期间已出现的交互优先完成；炼狱界直接交互，未进入地图导航。
三只猫各交互一次后结束，印章始终 7/7；通过槽位实际变化确认完成，覆盖仍为 34/74。

证据位于 `debug/cat/2026-09-27/initial/`、`fixed1/`，移动按钮原生样本见
[移动猫按钮回放](../../../tools/fixtures/cat_diary_moving_button.md)。
80 项原有测试、5 项移动交互边界测试、5 组移动按钮原生回放、4 组普通对话/海报回放、Schema 和 maa-tools 检查通过。
最终运行包源码及资源与成功运行快照一致；27 张缺失源码模板按初始哈希原样恢复，运行包模板未丢失，原因未确定。
本次为分段修复完成，最终版本从剩余一项验证至完成；初始三项整轮与 GUI 按钮入口未验证。
