# 猫咪日记

GUI 任务“猫咪日记”从主界面或已打开的日记页开始，读取三个猫槽位的线索，匹配地点后传送，
默认通过小地图追踪角色和羽毛，首次展开区域图建模，必要时再展开校准，在场景内点击羽毛按钮。每次交互后重新读取全部三项任务，
继续处理猫的新地点，直到三个槽位都出现红色完成爪印。

共享定位、移动目标时效、换层重建与无小地图场景的处理见 [小地图导航](minimap-navigation.md)。
接入后的实测边界见 [默认导航接入报告](minimap-integration.md)；旧单帧定位仅留作历史截图诊断，不是运行时回退路径。

## 使用

- 国服、ADB 控制器、16:9 横屏，控制器缩放截图为 1280×720。
- 已解锁目标地点的传送点；开始前退出战斗、世界地图和其他功能页。
- GUI 可选择 15、45（默认）、90 分钟运行上限；超时后保留现场并报告失败。
- 移动目标需要及时截图，MuMu 使用客户端自动检测出的截图和输入方式。
- 任务不按固定“每只猫两次”或印章数量判断完成，某只猫可以在一次交互后结束。
- 印章卡包含礼物位置上的第七枚；满卡可能保持 7 枚并继续更新线索。已实测换新卡时的 `7 → 1`，不将其当作每日刷新。
- 上方倒计时属于当日日记，下方倒计时属于印章奖励。跨过当日日记的期限时停止追踪旧线索。

```powershell
python -X utf8 tools/run_startup.py --adb-path 'E:\Program Files\Netease\MuMu\nx_main\adb.exe' --address 127.0.0.1:16384 --task CatDiary
python -X utf8 tools/test_cat_diary.py
```

命令行使用默认 45 分钟上限，日志位于 `debug/cat-diary-run/`。
退出码 0 要求执行到 `CatDiaryComplete`，失败为 1，Ctrl+C 停止为 130。
GUI 资源更新后需要重新运行 `tools/package_gui.py`，使 Agent、地点数据、模板和任务选项一起进入运行包。

## 实现与资料

| 文件 | 职责 |
| --- | --- |
| `assets/resource/pipeline/cat_diary.json` | 日记、地图、确认框、图例、对话与完成态识别，以及点击和滑动动作 |
| `agent/cat_diary.py` | 三槽位状态、地点消歧、道路规划、移动目标追踪、进度复核 |
| `agent/data/cat_diary.json` | 用户提供的两张地点对照表，共 76 条；实机补充文字变体和传送名称 |
| `agent/data/cat_diary_maps.json` | 实际道路中心线、横向斜坡、地图定位锚点及图例遮挡时的有限搜索点 |
| `tools/test_cat_diary.py` | 地点消歧、刷新、超时、停止、路径与确认框边界验证 |
| `tools/fixtures/cat_diary_stamps.npz` | 实机印章卡局部样本，覆盖 5 枚、第 7 枚和换卡后第 1 枚 |
| `tools/fixtures/cat_diary_konium.npz` | 柯尼姆实机区域图局部样本，覆盖黄色任务图标与角色环重叠 |
| `tools/fixtures/cat_diary_kunlun.npz` | 昆仑山脉小尺寸任务菱形与角色环局部样本，验证排除任务标记后的角色定位 |
| `tools/test_cat_diary_dialogue.py`、`tools/fixtures/cat_diary_poster.npz` | 体育场海报两页、普通对话与负场景回放，核验恢复和页数上限 |

图片素材来自用户提供的日记、地点对照表、羽毛图例，以及 2026-09-07～15 的 MuMu 实机截图。
用户图片中的表格作为地点资料使用。模板基准为 1280×720；场景内羽毛按钮会随景深缩放，使用多尺寸模板。

新增线索时录入有区分度的原文片段，`world_names` 收录实际世界地图及确认框中出现的完整名称。
不要仅凭相似文字决定目的地。OCR 可能把城镇前缀和名称分成多个框，确认框第一行按横坐标合并后，
必须与允许的完整目的地一致，且同时识别“是否确定”和“是”按钮才点击。
确认文字先按亮色过滤，避免遮罩后方的城市标签被 OCR 拼进确认内容。
确认框第二行“是否确定？”也使用相同过滤；2026-09-13 实机观察到背景“麻帆良湖”的文字
被拼成“麻帆是否确定？”，过滤后能按原有完整正则识别。目的地、确认句和按钮仍需同帧通过。
单行确认文本过滤高度不足正文一半的独立 OCR 小碎片，避免埃尔吉昂句末标点被读成“2”；
正常字号的数字仍保留，完整目的地匹配要求不变。
区域图标题保留原始抗锯齿文字，必要时用灰度图补充识别，再按横坐标合并，避免标题分框顺序颠倒；
纳兹里克补充实机标题 OCR 的“茲”字变体，仅用于落地地图名校验。
2026-09-25 帕德列传送后识别到实机完整标题“古代树之村帕德列”，但旧 `map_names` 仅登记“帕德列”，
无法通过共享导航的精确地图校验。将该项改为实机全名，保留既有道路、传送名称和精确匹配规则。
标题局部样本收录至 `minimap_titles.npz`；回放复现旧配置失败，并验证新配置接受完整标题、拒绝简称和附加楼层。
点击 OCR/模板识别框的中心，避免框外留白导致随机点击落空。选择时代前先检查选中标记，
当前时代已选中时不重复点击；时代标记与广域入口使用对亮度敏感的模板方法，排除确认框遮罩后的背景。
时代指针阈值为 0.95，排除展开动画中约 0.86 的背景误命中；切换时代后等待广域按钮可见，
再使用同一帧的识别结果点击，避免指针先出现而按钮仍在过渡。
冥峡界从“???”时代的广域地图进入，识别“冥峡界”分区后再选择目的地，沿用完整传送确认校验。

柯尼姆经“现代 → 本土大陆 → 蛇骨岛”进入弹出地图，实际传送名称为“魔兽村落 柯尼姆”。
地点数据的 `world_entry` 指定外层入口；独立弹图另外用 `world_panel` 指定识别节点，城镇子菜单不设置此字段；
`world_names` 仍只接受最终目的地，不把蛇骨岛作为村落的传送别名。
展开动画期间最多等待 10 秒，同帧识别分区标志和最终目的地标签后再点击，
然后继续执行原有完整确认文字校验。`SnakeIsland.png` 来自实机弹图的固定文字区域 `[635,149,110,28]`，
已检查蛇骨岛正样本、外层世界地图及魔兽城子菜单负样本。

蛇头梅兹基塔同样从蛇骨岛弹出地图进入。2026-09-20 实机发现该传送按钮位于 `y=115` 附近，
原本土地图目的地 OCR 从 `y=182` 开始，无法读到顶部按钮。岛内改用独立的
`CatDiaryIslandDestination` 范围 `[80,90,1100,550]`，仍要求同帧通过蛇骨岛标志，且最终传送确认精确匹配目的地。
实机截图回放确认顶部蛇头按钮、中部柯尼姆按钮均可识别，错误名称和外层地图不能通过对应检查。

蛇头梅兹基塔没有小地图，`chase_mode: snake_head` 只沿实走的传送落点向左移动一次（600ms）。
前后识别远景树及夕阳海面，要求起点和向右的场景位移符合已观察范围；未知场景、错误起点、无位移或反向位移均停止。
移动后最多等待 3 秒让羽毛出现动画结束，再执行原有交互与日记变化复核；已验证路段内找不到猫时保留现场。
`SnakeHeadTree.png` 取自落点 `[330,335,60,55]`，一次左移后的匹配位置为 `(417,335)`，得分 0.965。
该处羽毛较小，旧模板最高得分 0.885，补充完整按钮样本 `FeatherSunset.png` `[563,314,35,35]`，沿用 0.9 阈值。
两个模板在 11 张宫殿、日记及柯尼姆负样本中最高得分分别为 0.525、0.419；其他移动范围尚未验证。

2026-09-27 佐见复现到移动羽毛按钮连续点击落空：全画面多尺寸识别完成时，按钮已离开旧点击位置。
`refresh_cat_button` 现在在每次点击前读取新截图，确认菜单和地图按钮仍可见，
再在旧羽毛框左右各 120px、上下各 60px 的区域重识别，范围限制在原场景 ROI 内。
模板与 0.9 阈值保持不变；只点击新识别框，按钮消失或画面切换时不使用旧坐标，也不按速度推测位置。
五组实机采样中，全画面截图及识别耗时 0.436～0.451 秒，局部耗时 0.048～0.051 秒；
前四组按钮已横移 40～42px，第五组消失。保留 10 次点击与 40 秒交互上限，并以返回日记确认进度。
样本与回放说明见 [移动猫按钮回放](../../../tools/fixtures/cat_diary_moving_button.md)。

萨露帕使用古代本土地图右上角的直接传送点。2026-09-12 复现到原扫描起点落在
帕西法尔宫殿和拉托尔的地点按钮内，连续拖动未移动地图，导致漏扫北侧。
扫描现在读取同帧地点文字，将文字框横向扩展 100px、纵向扩展 25px 作为按钮避让区，
在地图内容区选择最近的可用起点，保持原有方向、距离和扫描次数上限；
标签不可读且地图控件未就绪，或没有可用起点时停止。修复后运行包已自动传送萨露帕并开始村内追踪。
2026-09-15 观察到未来东方地图边缘可只有海域、没有任何地点文字；此时要求地图标题、当前时代指针和广域按钮同帧可见，
才允许从地图内容区继续原有最多 34 次拖动扫描。弹窗遮罩或时代过渡仍停止，不增加重试上限。

2026-09-13 进一步观察到，世界地图标题和广域按钮可先于时代指针出现。
切换时代前新增 `CatDiaryEraPointerReady`，等待任一时代的白色指针达到原有 0.95 阈值，
再用同一帧判断当前时代。避免展开动画中重复点击当前时代，穿透到后面的地点按钮；
指针在 10 秒内仍未就绪时停止，不降低识别阈值。

“这个村子里面有好多好吃的食物”对应斑鸠之乡。2026-09-13 重新核对用户提供的东方地点表，
更正原先误录的“贽殿之乡”，保留 `cat_63` ID；运行包已实测传送斑鸠之乡，使用现场差分道路追踪 2 步后完成交互。

## 寻路与退出条件

右上小地图随镜头移动，不能将其屏幕坐标当成世界坐标。进入地图后展开区域图建立模型，有整体平移时先用固定图例校正；
之后从小地图的呼吸环和静态道路恢复角色位置，并更新移动中的羽毛，短距离滑动后再观测实际位移。
证据不足、目标过期或每 45 秒核对身份时再展开整图；旧单帧角色模板不是正式流程的回退路径。
已录入地图使用道路中心线；其他地图尝试从同一机位主界面与展开图的差分提取道路，
只在识别出的图例遮挡范围内连接已有道路两端，或连接已定位的角色与同轴可见路面。
移动后优先复用此前已产生正确位移的同图道路，避免角色环遮住路口时改走绕路、离开后又折返；
旧道路无法规划或移动受阻时重新使用本帧道路，猫的位置仍逐帧更新。
路口处横向误差不超过 8px，且后续是超过 8px 的纵向连接路时直接换道，避免最短横移越过路口。
路宽内的短折线不作为换道口，远离路口时仍先横向接近。
2026-09-24 伊斯亚实测发现，路径先包含路宽内的短纵向投影、再接短横向对齐时，
旧分支跳过投影后未继续检查换道，导致路口附近左右微调。现在对忽略投影后得到的横向短段
继续应用原有 8px 路口容差，且仍要求后续连续纵路超过 8px；不扩大转弯范围。
路径不连通时停止，不直线穿越空白区域。

巴尔沃基中央道路已根据 2026-09-12 实机区域图标定，覆盖本次从中央传送点向西追猫的道路；
图例密集时使用该道路网络，避免现场差分把路口切断。本次运行包实测 5 步完成交互。
西北斜路等未走通的分支未加入标定。

纳兹里克本次只标定东侧传送点到猫所在道路的连接路段，西侧和其他路线未标定。
其两段斜坡经实机确认由左右输入通过，按实际坡面录入 `horizontal_slopes`，不把像素路径的台阶当成上下换道。
该地图还会随坡面高度整体纵移，使用固定心形图例将角色和猫的位置还原到标定坐标；
锚点不可见、横向偏差超过 8px 或纵向偏差超过 45px 时停止定位，不沿用错误坐标。

已标定地图会排除道路附近以外的角色环候选，防止背景灯笼被当成角色。
新定位用多帧圆周变化区分角色呼吸环和静态任务菱形；遮挡或重叠仍无法消歧时重新采集整图或停止。
历史诊断保留的柯尼姆模板 `QuestIcon.png` 取自 `[429,245,31,38]`，不代替新定位的时序证据。
关图后若角色已接近羽毛、或羽毛与其他图例重叠而漏识别，会再次检查场景内的交互按钮。
2026-09-22 安格尔实测按钮可能在移动后的导航采样期间才出现，若继续开图，重叠羽毛会使角色环定位停止。
追猫阶段在导航采样及开图前也识别主界面和羽毛按钮，优先调用原交互流程；必须实际回到日记，
随后仍复核线索、印章或槽位变化。地图展开时和跨层前往目标的阶段不触发该交互，保留原识别阈值。
加达洛已观察到羽毛被任务图例长时间遮住，允许沿已确认道路访问一个已观察到猫的搜索点 `(422,392)`；
仍逐步重新识别移动羽毛，抵达搜索点不代表成功。该点用尽仍未找到羽毛或交互按钮时，按四帧缺失保护停止。
搜索点仅对有道路中心线的指定地图生效，与普通追踪共用步数、无位移、用户停止和时间上限。

2026-09-20 伊刹那的羽毛被黄色任务图标遮挡；现场放大区域图确认其位于 `(880,323)` 附近。
仅标定传送点所在的 `y=409` 横路、`x=706` 连接路及 `y=323` 上方横路，沿右、上、右前往该搜索点。
复用上述有限搜索和实际交互确认，不将黄色任务图标本身作为羽毛模板，其他街道未标定。
起点彩色标题曾将“巳”识别为“已”，使导航锁定错误全名、后续正确识别反而被拒绝。
该地点现在显式声明完整 `map_names: ["巳之国伊刹那"]`，在首次建模时即调用既有灰度补充识别，
保留后续全名精确校验；起点与东侧两份真实标题局部样本加入 OCR 回归。

2026-09-22 扎博的现场差分道路在传送点到下一条街道之间断开。依据实机区域图，仅标定
`y=231` 从 `x=484` 到传送点附近 `x=565` 的横路、`x=484` 至 `y=317` 的连接路，
以及 `y=317` 上 `x=484～595` 的横路，沿左、下、右路线接近猫；其他街道和分支尚未标定。

冻时领域东侧没有直接传送按钮，地点配置 `approach: nazrik_east` 先传送纳兹里克。
仅从已确认的东侧落点 `(943,284)` 附近出发，沿实机走通的右侧道路与斜坡移动，
镇内优先读取小地图；无法继续配准时核对当前区域图，无小地图连接路仍需识别绿色灯具模板 `NazrikEastPassage.png`。
最多移动 18 步，只有重新展开区域图并读到完整“冻时领域”标题才继续追踪猫；
错误起点、陌生地图、连接路场景不符、用户停止或步数耗尽均失败。
2026-09-12 运行包实测 12 步跨图、5 步追猫后完成交互；东侧道路使用现场差分提取。

异元晶控制所使用游戏内完整名称传送，旧线索目录中的“异次元控制所”仅保留为地点记录名。
`approach: xeno_research` 从入口沿右上道路进入研究中心，再沿右上道路点击扶梯进入深处。
入口、研究中心下层和上层分别标定道路；后两层标题相同，因此使用各自的落点和出口坐标核验跨层。
每段最多 40 步，连续三步没有预期位移时停止；只在出口坐标 10px 内识别门图标并点击。
点击后先等待菜单消失，确认开始切换，再等待新场景可操作，避免旧场景尚未淡出时抢先开图。
最后还需确认研究中心上层落点 `(498,462)`，才开始追踪羽毛。
门模板 `XenoDoor.png` 来自入口右侧房门图标 `[493,175,44,47]`，研究中心扶梯上的同类按钮已核对。
2026-09-12 运行包实测两段各移动 10 步，完成房门、扶梯切换及上层落点验证后，11 步追猫完成交互。

“博物馆里有好多书”对应马克米纳尔博物馆的图书区域，可从未来本土大陆的“尼尔瓦”子菜单直接传送。
`world_entry: 尼尔瓦` 只用于展开菜单，`teleport` 和 `world_names` 均为“博物馆 图书区”；
只接受确认文字“即将移动到博物馆 图书区”，不能把尼尔瓦或博物馆正门当作目的地。
子菜单仅点击一次，随后等待确认框；图书区标签与“是”按钮的位置重叠，动画中的旧菜单帧不可触发连点。
落地后核对完整地图名“马克米纳尔博物馆图书区域”，沿已有道路继续追猫，实测落点约 `(802,264)`。
图书区道路仅对应入口所在区域，不覆盖更深处同名区域。
2026-09-26 初次实现误判为需要逐层进入；用户提供子菜单截图后已删除七段进场路线及专用门、楼梯、黑屏资源。
子菜单、确认框和完整标题样本见 [博物馆直达回放](../../../tools/fixtures/cat_diary_museum.md)。

米格兰斯城二楼配置 `approach: miglance_second_floor`。传送后先核验“米格兰斯城1楼”和落点 `(640,410)`，
识别正前方楼梯图标并点击，等待旧场景退出和新场景恢复，再核验“米格兰斯城2楼”及对应落点。
两次落点容差均为 20px，楼层不符时停止。`MiglanceStairs.png` 取自一楼实机图标 `[620,161,52,49]`，
一楼正样本与二楼同区域负样本得分分别为 1.0、0.279，阈值 0.9。
二楼中央楼梯图标会干扰差分道路提取，使用实机标定的两侧连接路，沿右侧通道进入上方走廊追猫。
2026-09-13 运行包实测从一楼自动上楼，核验二楼落点后追踪 7 步完成交互。

温泉使用现代东方地图中怨丹之原左下方的“温泉”直接传送点，修正原先绕到伊刹那的配置。
排查时确认伊刹那实机传送名称为“巳之国 伊刹那”；这只验证传送名称，不计入该地点的寻猫完成数。
温泉没有小地图，`chase_mode: hot_spring` 从落点向左沿池边寻找场景内的羽毛按钮。
每步先识别入口岩壁、池边树木或浴场招牌，按场景顺序和同一标志的横向位移核验移动；
场景标志不符、顺序倒退、连续三步没有预期位移、用户停止或期限到达时停止，最多滑动 8 次。
每步复用现有羽毛交互与回日记流程，看到招牌或走满步数都不代表完成。
三个 `HotSpring*.png` 模板分别取自实机入口 `[868,165,122,124]`、树木 `[486,89,115,107]`、招牌 `[147,180,67,74]`，
在上半屏匹配，阈值 0.9。已检查沿途正样本及伊刹那场景负样本；2026-09-15 运行包从传送点开始 4 步完成交互。

空中城郭伊斯亚的传送落点位于 `(749,281)` 附近，本次猫在下方一条道路的西段。
场景背景和图标会干扰差分提取，曾在多个没有路口的位置尝试向下，最终按三次无位移保护停止。
依据实机区域图标定两条横路 `y=281/360` 和 `x=589` 的连接路，从落点先向左，再向下，最后向西追猫。
2026-09-15 运行包重新传送后，在第 12 次定位轮完成追踪（实际滑动 11 次）；其他层道路未标定。

昆仑山脉使用 `approach: kunlun_slide`：先传送妖魔殿，核验落点 `(653,394)` 附近，沿当前横路向右。
每步检查地图名、横路范围和实际位移，最多移动 6 步，三步无位移时停止。
只有接近右侧出口 `x>=695` 才识别冰坡感叹号并点击；随后等待切换，并核验“昆仑山脉”及落点 `(598,598)`，容差 20px。
`KunlunSlide.png` 取自妖魔殿右侧 `[853,301,64,64]`，阈值 0.9；已检查入口、远离出口和滑下后场景负样本。
昆仑山脉的羽毛和任务菱形尺寸不同于旧样本，补充 `MarkerKunlun.png` `[877,557,38,37]` 与
`QuestIconKunlun.png` `[525,444,27,32]`，分别沿用原有 0.82、0.85 阈值。
先排除任务菱形，再用现有金色角色环补充定位；不降低角色定位阈值，也不把任务标记作为路点。
2026-09-15 运行包实测 2 步到冰坡、确认滑下后 15 步完成寻猫；山路使用现场差分提取。

猫神神社遗迹的实机世界地图、传送确认框和区域图均使用“遗迹”，修正资料录入的“遗址”。
传送落点在 `(668,445)` 附近；区域图背景与角色图标会使自动道路提取不连通，
依据实机截图标定 `y=445/360/275` 三条横路及 `x=590/730` 两条连接路，沿左、上、右、上、左路线追猫。
2026-09-18 运行包从传送点开始滑动 13 次完成交互。

2026-09-23 首次完成水之都阿克托尔的猫咪日记交互。从传送落点 `(503,400)` 附近沿当前横路向右，
经 `(611,400)` 附近连接路上行，再沿 `y=313` 横路向左追猫；现场差分提取道路，未新增固定中心线。
运行包实测 10 次滑动、2 次整图建模/校准、8 次小图更新，交互后首槽红爪、印章从 4 增至 5。
仅计入本次寻猫路线的完成覆盖，不代表该地图其他街道或出口均已验证。

帕西法尔宫殿修正原资料中的“帕萨法尔宫殿”，实际传送进入一楼，落点在 `(640,457)` 附近。
一楼背景与密集图标使自动道路提取不连通；按实机小地图标定 `y=457/360` 两条横路及 `x=562` 的连接路，
从落点先向左，再向上到花坛附近追猫。2026-09-19 运行包重新传送后滑动 4 次完成交互；其他走廊和楼层未标定。
宫殿明亮地面还会使 `StartUpWorldReady` 的地图按钮模板得分降至 0.899，低于 0.9 阈值；
补充 `Startup/WorldMapBright.png` 实机模板，专用阈值 0.97，保留旧模板 0.9 和菜单、地图同时可见的要求。

地点库覆盖范围不等于全部地图都已实测。冥峡界目前验证纳兹里克入口和通往冻时领域东侧的路线；
冻时领域西侧、其他未列出的跨区域出口及楼层切换尚未验证；
未解锁传送点、无法匹配的线索、看不到羽毛、无法确定道路或角色位置时，任务报告原因并保留现场。
未知场景的自动道路提取仍需更多实机验证。

每个地点最多 240 次定位移动，连续三步没有预期位移即停止。每轮最多 24 次猫事件，
每次普通对话和已识别的体育场海报说明合计最多 20 页；只处理这些已采集页面、公告、战斗攻击和结算页。
所有截图和操作检查用户停止状态、任务上限及当日日记期限。

普通对话同时要求右上角“对话记录”文字和下方对话框边框匹配。
2026-09-16 佐见稻草人对话的明亮海滩背景使旧边框模板得分降至 0.813，低于原有 0.88 阈值。
补充实机裁剪 `DialogueBorderBeach.png` `[5,521,115,14]`，保留旧模板、阈值和双重识别条件。
新模板在该对话正样本得分 1.0，25 张其他场景及区域图样本最高 0.584；
运行包从卡住的对话恢复后，重新读取日记并连续完成佐见与莎蒂拉体育场。

2026-09-25 体育场追猫交互期间进入海报介绍。该界面没有右上“对话记录”，说明框位于顶部，
旧普通对话识别无法处理，最终按 40 秒交互上限停止；不能将进入海报当成猫交互成功。
实机采集并逐页确认两页文字：“循着来自宇宙的通讯”和“宇宙的神秘以及”，点击两次后返回场景。
新增 `CatDiaryStadiumPoster`，同帧要求顶部边框和对应首行完整文字；`CatDiaryDialogue` 将其与原普通对话分支并列。
边框模板裁自实机 `[152,22,57,60]`，采用亮度敏感匹配，阈值 0.9；普通对话原有双重识别条件不变。
共享原有 20 页、任务期限与停止检查，关闭海报之后仍必须实际回日记并确认线索、印章或槽位变化。
样本来源及正反例见 [对话回放样本](../../../tools/fixtures/cat_diary_poster.md)。

日记读取要求连续两次三槽位状态一致；单个槽位读不到文字不代表该猫结束。
每次交互都复核任务或印章的实际变化；仅接受已观察到的 `7 → 1` 满卡换新卡，其他印章减少仍停止。
完成的猫重新变成待找状态或任务与印章均没有变化时停止，满卡换卡也不能绕过每日刷新期限和槽位重置保护。
完成节点再次独立识别日记标题、面板、可见的关闭键与三个红爪印，避免 Python 动作返回成功但游戏任务未完成。

## 验证记录

环境：MaaFramework 5.12.3，MuMu ADB `127.0.0.1:16384`，原生 2560×1440 缩放到 1280×720。

| 日期 | 实机结果 |
| --- | --- |
| 2026-09-07 | 最终之岛、海之国佐见、猫神神社、劳拉巨蛋城完成寻猫交互；验证刷新地点、普通对话后回日记与单次交互即结束的猫。当天整轮未完成。 |
| 2026-09-08 | 分段调试完成帕德列 → 莎蒂拉体育场、王都尤尼冈、萨露帕 → 浮空城尼尔瓦，共 5 次交互，三只猫全部显示红爪印。最后使用正式命令行入口运行剩余任务，达到 `CatDiaryComplete`，退出码 0。 |
| 2026-09-09 | 分段调试完成伊杜依斯、那古萨无 → 猫神神社、加达洛，共 4 次交互，三只猫全部显示红爪印。加达洛在正式命令行入口通过遮挡搜索找到猫，达到 `CatDiaryComplete`，退出码 0。验证满卡后 `7 → 1`、移动图例重叠、场景交互晚于地图识别出现和灯笼误识别；补充加达洛“凉爽的山风”原文。 |
| 2026-09-10 | 分段调试完成埃尔吉昂·伽玛区 → 火之村拉托尔、扎博、破晓之岛 → 影之镇纳兹里克，共 5 次交互，印章由 2 增至 7，三只猫全部显示红爪印。前两只猫由 GUI 运行包独立 Agent 连续完成；修复后，纳兹里克从传送点开始的独立 Agent 任务连续执行到 `CatDiaryComplete`，退出码 0。验证传送点中心点击、当前时代跳过重复选择、冥峡界入口、路口遮挡折返、最短滑动越过路口、斜坡与地图纵移校正，以及标题背景干扰和“茲”字 OCR 变体。 |
| 2026-09-11 | 使用提交 `266935f` 对应的 GUI 运行包，从三只猫均未完成的日记页启动独立 Agent，一次连续无干预完成埃尔吉昂·伽玛区、王都尤尼冈、辰之国那古萨无。三只猫均一次交互即结束，共 3 次交互，印章依次为 `7 → 1 → 2 → 3`，最终三个槽位均有红爪印。达到 `CatDiaryComplete`，退出码 0；本轮未修改任务代码或资源。复核满卡换卡、单次交互结束、跨时代与同一时代切换大陆、标题识别及路口换道。 |
| 2026-09-12 | 分段修复并完成柯尼姆 → 最终之岛 → 王都尤尼冈 → 冻时领域东侧，巴尔沃基 → 萨露帕 → 异元晶控制所，安格尔，共 8 次交互。印章从 3 增至 7 后保持满卡，后续以线索和槽位实际变化确认交互。修复蛇骨岛分区、任务图标误识别为角色、时代切换过渡、地点按钮吞掉地图拖动、巴尔沃基道路断开、冻时领域跨图和控制所同名楼层导航。最后一次运行包独立 Agent 从剩余两项任务连续完成异元晶控制所和安格尔，三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0；当天并非从初始三项一次无干预完成。 |
| 2026-09-13 | 分段修复并完成米格兰斯城二楼、隐居村落伊杜依斯、斑鸠之乡，共 3 次交互，印章保持 7/7。修复地图展开时过早点击时代按钮、城堡一楼到二楼的楼梯导航与道路误识别、斑鸠之乡地点误录，以及传送确认句被背景文字干扰。运行包先连续完成城堡上楼及 7 步追猫、伊杜依斯 9 步追猫；修复后另一次运行从剩余斑鸠之乡任务开始，自动传送并追踪 2 步，三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0。当天并非从初始三项一次无干预完成。 |
| 2026-09-14 | 使用提交 `05ff214` 对应的 GUI 运行包，从三项均未完成的日记状态启动独立 Agent，一次连续无干预完成影之镇纳兹里克、辰之国那古萨无、王都尤尼冈，分别追踪 13、9、8 步。三只猫均一次交互即结束，共 3 次交互，印章保持 7/7，三个槽位均有红爪印。达到 `CatDiaryComplete`，退出码 0；本轮未修改任务代码或资源。复核冥峡界传送、纳兹里克斜坡及纵移校正、同图标题 OCR 变体、跨区域切换和满卡时按槽位变化确认完成。 |
| 2026-09-15 | 分段调试完成帕德列、温泉、埃尔吉昂·伽玛区 → 空中城郭伊斯亚 → 再生村落安格尔 → 昆仑山脉，共 6 次交互，印章从 0 增至 6。新增温泉直接传送与无小地图追踪、伊斯亚道路标定、妖魔殿冰坡跨图和昆仑山脉小尺寸图例；修复空白海域被当成 OCR 失败。运行包验证温泉 4 步、伊斯亚 11 次滑动、安格尔 7 步；最后一次独立 Agent 从剩余昆仑山脉任务开始，重新传送妖魔殿，2 步抵达冰坡并完成跨图，15 步追猫后三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0。途中发现部分原有源码及运行包图片缺失，已恢复并重新校验；缺失原因尚未确定。当天并非从初始三项一次无干预完成。 |
| 2026-09-16 | 完成巴尔沃基、海之国佐见、莎蒂拉体育场，共 3 次交互，印章依次为 `6 → 7 → 1 → 2`。初次运行完成巴尔沃基后，因海滩背景干扰对话边框识别，停在佐见稻草人对话。补充海滩边框模板后，运行包独立 Agent 自动翻过现场对话，重新读取日记并传送，连续完成佐见和莎蒂拉体育场，三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0。复核普通对话恢复、满卡换卡及已完成槽位保留；当天并非从初始三项一次无干预完成，未增加新地点覆盖。 |
| 2026-09-17 | 使用包含 2026-09-16 海滩对话修复的 GUI 运行包，从三项均未完成的状态启动独立 Agent，一次连续无干预完成浮空城尼尔瓦、王都尤尼冈、猫神神社，分别滑动 6、7、1 次。三只猫均一次交互即结束，共 3 次交互，印章依次为 `2 → 3 → 4 → 5`。最终三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0；本轮仅补充验证记录，未修改任务代码或资源。复核跨时代传送、路口换道及逐次读取日记确认进度，未增加新地点覆盖。 |
| 2026-09-18 | 分段修复后完成猫神神社遗迹、浮空城尼尔瓦、空中城郭伊斯亚，共 3 次交互，印章依次为 `5 → 6 → 7 → 1`。修正猫神神社遗迹被录为“遗址”导致找不到传送点的问题；传送后自动道路提取不连通，依据实机区域图标定三条横路及两条连接路。最后一次运行包独立 Agent 从三项均未完成的日记开始，重新传送并连续完成全部三项，分别滑动 13、6、10 次，最终三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0。新增一个已完成地点；当天并非使用初始版本一次无干预完成。 |
| 2026-09-19 | 分段修复后完成猫神神社、劳拉巨蛋城、帕西法尔宫殿，共 3 次交互，印章依次为 `1 → 2 → 3 → 4`。初次运行连续完成前两项；修正宫殿名“帕萨法尔”为“帕西法尔”，补充一楼道路，并解决明亮地面导致主界面地图按钮漏识别的问题。最后一次运行包独立 Agent 从剩余宫殿任务开始，重新传送一楼，滑动 4 次完成交互，最终三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0。新增一个已完成地点；当天并非从初始三项一次无干预完成。 |
| 2026-09-20 | 分段修复后完成蛇头梅兹基塔、巳之国伊刹那 → 炼狱界、海之国佐见，共 4 次交互，印章依次为 `4 → 5 → 6 → 7 → 7`。补齐蛇骨岛入口及顶部目的地 OCR，新增蛇头无小地图的一步场景导航和小尺寸羽毛，补齐伊刹那遮挡搜索路线及完整标题校验，修复佐见移动羽毛误作角色呼吸环。运行包实测蛇头左移 1 次、伊刹那 10 次滑动后完成；炼狱界完成交互，佐见重新传送后实际滑动 10 次完成。最终三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0。新增 3 个已完成地点；当天并非从初始三项一次无干预完成。 |
| 2026-09-21 | 使用包含 2026-09-20 修复的 GUI 运行包，从模拟器桌面自动启动游戏，并从三项均未完成的日记状态一次连续无干预完成猫神神社遗迹、帕西法尔宫殿、劳拉巨蛋城 → 炼狱界，共 4 次交互。前三处分别滑动 14、4、8 次；炼狱界到达后完成交互，未进入小地图追踪。印章始终为 7/7，每次按线索或槽位实际变化确认进度。最终三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0；运行期间任务源码及资源哈希未变化。本轮未修改任务代码或资源，未增加新地点覆盖。 |
| 2026-09-22 | 分段修复后完成劳拉巨蛋城 → 魔兽村柯尼姆、再生村落安格尔、扎博，共 4 次交互，印章依次为 `0 → 1 → 2 → 3 → 4`。劳拉实际滑动 8 次；柯尼姆接近猫后重新提取道路受角色环与邻近图例遮挡，增加同图独立定位核验后的旧道路复用，重新传送后 7 次移动完成，并实际触发该修复。安格尔增加导航采样及开图前的场景交互检查，重新传送后 6 次移动完成。扎博补齐传送点至下一条街道的局部道路，最后一次运行包独立 Agent 从剩余扎博任务开始，重新传送、5 次移动后完成。最终三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0；当天并非从初始三项一次无干预完成，未增加新地点覆盖。 |
| 2026-09-23 | 使用包含 2026-09-22 修复的运行包，从游戏标题画面启动，经公告处理进入主界面，从三项均未完成的日记状态一次连续无干预完成水之都阿克托尔、埃尔吉昂·伽玛区、莎蒂拉体育场 → 隐居村落伊杜依斯，共 4 次交互，分别滑动 10、4、5、11 次。印章依次为 `4 → 5 → 6 → 7 → 1`，最终三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0。首次完成阿克托尔寻猫，新增 1 个地点覆盖；当天未修改任务代码或资源，首尾源码及资源哈希一致。 |
| 2026-09-24 | 初次启动在关闭公告后停留于梅纳斯试炼选关页；补充识别返回分支后，运行包自动关闭一次并进入主界面，从三项均未完成的日记连续完成空中城郭伊斯亚、巴尔沃基、米格兰斯城二楼，共 3 次交互，分别滑动 15、3、6 次。印章 `1 → 2 → 3 → 4`，最终三个红爪印，达到 `CatDiaryComplete`，退出码 0。复核城堡上楼与小图轮廓定位；未增加地点覆盖。日记这一轮使用启动修复版，尚未包含运行期间另行修正的路口微调逻辑；当天并非使用初始版本一次无干预完成。 |
| 2026-09-25 | 分段修复后完成炼狱界、古代树之村帕德列 → 莎蒂拉体育场、埃尔吉昂·伽玛区，共 4 次交互，印章 `4 → 5 → 6 → 7 → 1`。修正帕德列显式地图名仍为简称的问题，补充体育场海报两页说明的识别与返回。成功交互分段分别滑动 0、4、5、3 次；最后一次运行包独立 Agent 从重新打开的海报首屏自动恢复，连续完成剩余体育场、埃尔吉昂两项，最终三个红爪印，达到 `CatDiaryComplete`，退出码 0。未增加地点覆盖；当天并非初始三项一次无干预完成。 |
| 2026-09-26 | 分段修复后完成那古萨无 → 纳兹里克、猫神神社遗迹 → 拉托尔 → 马克米纳尔博物馆图书区域、柯尼姆，共 6 次交互，印章 `1 → 2 → 3 → 4 → 5 → 6 → 7`，最终三个红爪印、`CatDiaryComplete`、退出码 0。旧实现通过 107 次进场移动及 16 次追猫完成图书区，随后 6 次移动完成柯尼姆。用户指出尼尔瓦子菜单有“博物馆 图书区”后，已改为直接传送并删除绕楼层流程；后续复测传送与区域内导航，不计新增猫交互。新增 1 个已完成地点；当天并非初始三项一次无干预完成。 |
| 2026-09-27 | 分段修复后完成埃尔吉昂·伽玛区、炼狱界、海之国佐见，共 3 次交互，印章保持 7/7。初次运行在佐见移动羽毛按钮处连续点击落空；新增点击前的当前截图局部重识别，失去按钮或离开主界面时不使用旧坐标。最后一次运行包独立 Agent 从剩余佐见重新传送，10 次追踪移动后一次点击完成交互，三个槽位均有红爪印，达到 `CatDiaryComplete`，退出码 0。未增加新地点覆盖；当天并非初始三项一次无干预完成。 |

当前道路中心线包含 26 个实际区域，含导航共用的晓之塔魔物巢穴、伊刹那、扎博，以及 2026-09-26 新增的博物馆图书区域入口；阿克托尔、破晓之岛、冻时领域东侧、安格尔和斑鸠之乡使用现场差分提取道路。
新增边界测试覆盖小幅标记偏差误触换道、路端被角色遮挡、
移动图例遮挡已走过的道路、迟到的奖励提示、刷新期限、未读到槽位、错误传送目标和用户停止。
2026-09-22 柯尼姆路端的角色环与相邻图例共同遮挡道路，刷新区域图时无法提取角色脚下的连续道路。
同一导航会话在地图全名、机位偏移不变，且本次整图独立定位仍落在旧道路附近时，复用此前已确认的道路；
首次建模、传送或跨层后没有旧模型、偏移变化或当前位置越出旧道路时仍停止，不扩大补路或定位容差。
真实截图回放检查确认框背景干扰、不同大小的羽毛、三槽位完成标记和子菜单入口。

80 项猫咪日记单元测试、73 项导航回归（含原有 8 项）、Pipeline/Interface Schema、maa-tools 检查通过；
GUI 运行包同步资源后验证 Agent 握手与动作注册。
2026-09-11、2026-09-14、2026-09-17、2026-09-21、2026-09-23 已验证 GUI 运行包的独立 Agent 从三项初始任务开始，一次连续无干预完成整轮。
该验证通过程序调用运行包的原生客户端和 Agent，尚未通过 GUI 前端按钮启动完整一轮；未实测地图与跨层路线仍按上述限制处理。
本机当日原始日志和首尾截图保留于忽略目录 `debug/cat/2026-09-11/`。
2026-09-12 分段调试截图及各次运行包日志保留于 `debug/cat/2026-09-12/`，最后成功运行位于其 `fixed9/` 子目录。
2026-09-13 分段调试截图及各次运行包日志保留于 `debug/cat/2026-09-13/`，最后成功运行及三红爪完成截图位于其 `fixed5/` 子目录。
2026-09-14 整轮无干预运行日志和三红爪完成截图保留于 `debug/cat/2026-09-14/initial/`。
2026-09-15 分段运行日志、模板正负样本及排错截图保留于 `debug/cat/2026-09-15/`，最后成功运行和完成截图位于 `fixed7/`。
2026-09-16 首次运行、对话识别排查及恢复后成功运行保留于 `debug/cat/2026-09-16/`，最后成功运行位于 `fixed1/`。
本轮 Pipeline/Interface Schema 与 maa-tools 1.0.23 校验通过；本机原有 pnpm 依赖链接无法遍历，使用忽略目录内同版工具的 hoisted 安装完成检查。
收尾时发现梅纳斯鉴定 11 张原有模板缺失，已从 HEAD 恢复并同步运行包；重新打包及校验后未再次缺失，原因尚未确定。

2026-09-17 整轮无干预运行日志及三红爪完成截图保留于 `debug/cat/2026-09-17/initial/`；109 张源码及运行包图片逐一核对一致。

2026-09-18 分段调试日志及地图截图保留于 `debug/cat/2026-09-18/`，最后连续完成三项的运行及三红爪截图位于 `fixed2/`。
本轮 75 项猫咪日记测试与 maa-tools 检查通过；检查时发现原有 `MarkerNazrik.png`、`NazrikHeart.png` 缺失，已恢复，原因尚未确定。109 张源码及运行包图片逐一核对一致。

2026-09-19 日志、宫殿地图及主界面识别回放样本保留于 `debug/cat/2026-09-19/`，最后成功运行及三红爪截图位于 `fixed3/`。
本轮 75 项猫咪日记测试、8 项导航回归、Pipeline/Interface Schema 和 maa-tools 检查通过；110 张源码及运行包图片逐一核对一致。

2026-09-20 分段运行日志和截图保留于 `debug/cat/2026-09-20/`：`fixed4/` 完成蛇头，
`fixed6/` 连续完成伊刹那与炼狱界，`fixed8/` 从剩余佐见任务重新传送并完成，保留最终三红爪截图。
佐见移动羽毛产生第二个呼吸环候选的修复和回归见 [小地图导航](minimap-navigation.md)。
本轮 79 项猫咪日记测试、69 项导航回归、5 组真实 OCR、3 组羽毛投影及金色门按钮回放通过。
检查时恢复了 20 张意外缺失的已有图片，原因未确定；恢复后资源检查通过，114 张源码与运行包图片逐一核对一致。

2026-09-21 整轮无干预运行日志、启动和日记结果、最终三红爪截图保留于 `debug/cat/2026-09-21/initial/`。
其中 `source-manifest.json` 保存运行前源码与资源哈希，`summary.json` 汇总各段导航次数并确认运行期间无源码或资源变化。
当天仅补充实测记录，未重新运行上述单元测试；完整运行使用独立 Agent，未验证 GUI 前端按钮启动。

2026-09-22 分段日志、失败现场和地图截图保留于 `debug/cat/2026-09-22/`。
`initial/` 完成劳拉后停在柯尼姆；`fixed1/` 完成柯尼姆后停在安格尔；`fixed2/` 完成安格尔后停在扎博；
`fixed3/` 从剩余扎博任务完成至三红爪，退出码 0。各段 `summary.json` 统计实际移动和校准次数，
源码差异字段表示汇总时相对该次版本快照的变化，不作为运行期间是否修改文件的证明。
本轮 79 项猫咪日记测试、73 项导航回归、Schema 和 maa-tools 检查通过，运行包更新并验证 Agent 握手、注册及正常退出。
收尾检查发现 48 张已有源码图片缺失（含应用图标），已按 HEAD 原样恢复；原因未确定，恢复后重新检查资源及运行包一致性。

2026-09-23 整轮无干预运行日志、启动结果、首尾截图、版本快照和导航汇总保留于 `debug/cat/2026-09-23/initial/`。
本轮共 30 次追踪滑动、7 次整图建模/校准、24 次小图更新；同一 Agent 从初始三项执行至 `CatDiaryComplete` 后正常退出。
当天仅补充实测记录，未重新运行单元测试；源码及资源首尾核对一致，没有发现原有图片缺失。
使用的是程序调用运行包原生客户端及独立 Agent，尚未验证 GUI 前端按钮启动整轮。

2026-09-24 启动失败现场位于 `debug/cat/2026-09-24/initial/`，启动修复后连续完成三项的日志、截图和汇总位于 `fixed1/`。
共 24 次追踪滑动、6 次整图建模/校准、20 次小图更新；城堡的整图次数包含一楼与二楼各一次，小图含 1 次轮廓配准。
巴尔沃基小图暂时缺少可确认角色环时，重新展开整图恢复定位；未降低识别要求。
该轮运行包保持原有导航逻辑，期间源码新增路口修复，因此汇总显示 `agent/cat_diary.py` 与运行前快照不同。
打包前检查发现 15 张原有源码模板缺失，已核对当日初始哈希并从 HEAD 原样恢复；缺失原因尚未确定。
启动恢复的实机与离线回放边界见 [启动并登录](startup.md)。
随后更新运行包，另行从伊斯亚传送落点到下方横路 `(480,360)` 验证路口修复：
11 次滑动、2 次整图、11 次小图更新，中途无需重新校准；在 `(596.80,279.63)` 附近直接向下换道，未再次左右折返。
终点独立整图位置为 `(485.95,358.75)`，与最后小图观测相差 1.45px，达到 `NavigationMiniMapComplete`、退出码 0。
随后重新打开日记确认三个红爪与 4 枚印章；这次导航回归不计作新的猫交互或新地点覆盖。
证据保留在 `debug/cat/2026-09-24/isiya-route/`。本轮 80 项猫咪日记与 73 项导航回归、启动截图回放及资源检查通过。
最终版本未从当天初始三只猫重跑整轮，GUI 前端按钮入口仍未验证。

2026-09-25 证据保留于 `debug/cat/2026-09-25/`：`initial/` 完成炼狱界后停于帕德列标题校验，
`fixed1/` 完成帕德列后停于体育场海报，`fixed2/` 自动翻过两页海报并连续完成剩余两项。
排查时先逐页采集海报、确认关闭后返回场景，再重新打开首屏，验证运行包自动恢复；不计作猫交互。
成功交互分段合计 12 次滑动、6 次整图建模/校准、13 次小图更新，不含此前失败的体育场尝试。
体育场成功段的小图更新含 3 次轮廓配准；炼狱界直接完成交互，没有进入小地图导航。
80 项猫咪日记测试、6 组真实标题 OCR 回放、海报正反例与恢复/20 页上限回放、Schema 和 maa-tools 检查通过。
最终版本只从剩余两项验证至完成，尚未从当天初始三项完整重跑，也未验证 GUI 前端按钮入口。

2026-09-26 证据保留于 `debug/cat/2026-09-26/`：`initial/` 自动启动游戏，完成前四次交互后停于图书区域传送；
`fixed1/` 从剩余两个槽位重新读取日记、传送并连续完成，保留最终三红爪截图、版本快照和 `summary.json`。
成功交互分段合计 57 次追猫滑动、107 次博物馆进场滑动、30 次成功建模/校准、7 次出口整图复核、151 次小图更新；
不包含人工采样路线与世界地图拖动。拉托尔直接完成交互，没有进入小地图追踪。
旧跨层版本通过 80 项原有猫咪日记测试、11 项博物馆边界测试、14 个原生按钮/黑屏正反例及 1 组图书区域标题 OCR、Schema 和 maa-tools 检查。
当时运行结束时源码及资源与 `fixed1/` 快照一致；该快照不代表后续纠正后的直达版本。
发现缺失的 `MarkerNazrik.png`、`NazrikHeart.png`、`WorldMapBright.png` 已按原哈希恢复，原因仍未确定；
其中运行期间仅恢复了源码中的 `WorldMapBright.png`，运行包中该图片始终存在且哈希正确。
旧跨层版本从剩余两项运行至完成，未从当天初始三项完整重跑，GUI 前端按钮入口仍未验证。

同日用户指出直达入口后，`direct-library/` 使用更新运行包的 Agent 模块、资源和 MaaFramework 5.12.3，
分别从尼尔瓦、已在图书区两种状态完成直接传送，均确认目的地文字、完整区域标题和落点约 `(802,264)`，进场移动均为 0。
随后沿小地图走到此前寻猫的下方走廊：16 次移动、16 次小图更新，终点整图位置 `(454.53,455.24)`，
距标定目标 `(450,456)` 约 4.59px，小图与整图误差 1.92px。
80 项原有测试、5 项直达边界测试、5 组原生回放通过；旧跨层测试与专用资源已清除。
猫咪当日任务已经完成，直达复测只验证传送和区域内导航，没有重复猫交互，也未验证 GUI 按钮入口。

2026-09-27 证据保留于 `debug/cat/2026-09-27/`：`initial/` 完成埃尔吉昂与炼狱界，佐见首次追踪 9 步后因 10 次点击落空停止。
`fixed1/` 从剩余佐见重新传送，10 步追踪后一次点击完成，保留日记三红爪截图与 `CatDiaryComplete` 成功结果。
成功交互分段共 13 次移动、4 次成功建模/校准、11 次小图更新；不含佐见失败段的 9 次移动、3 次校准和 8 次小图更新。
三次交互印章均为 7/7，以实际槽位变化确认进度。80 项原有测试、5 项移动按钮边界测试、5 组真实按钮回放及 4 组对话回放通过，
Schema、maa-tools、运行包 Agent 握手及资源一致性检查通过。
资源检查发现运行前仍存在的 27 张源码 PNG 缺失，已验证运行前快照、HEAD 和运行包三者哈希一致后原样恢复；
运行包对应文件完好，缺失原因未确定，清单保存在 `restored-source-images.json`。
本日为分段修复后完成，最终版本只验证剩余佐见，未重跑初始三项或 GUI 前端按钮入口。

截至 2026-09-27，地点库 76 条线索对应 74 个不同地点（同一时代、分区和地点名去重）。
上述记录已完成 34 个地点的寻猫交互，尚有 40 个地点未完成实机验证：古代 18、现代 15、未来 6、冥峡界 1。
按线索条目计，34 条已实测、42 条未实测；克鲁利大道和米格兰斯城二楼各有两条线索，城堡另一条线索仍未抽到。
仅传送成功、经过某区域或录入道路数据不计入已完成地点。
